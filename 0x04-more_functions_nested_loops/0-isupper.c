#include "main.h"

/**
* _isupper - checks for uppercase character
* @c: the character that it checks
* Description: using the _isupper function
* this program should upper case
* Return: always success
*/
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
