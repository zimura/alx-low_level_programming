#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: the character to print
* Description: using the _abs function
* this proogram should computes the absolute value of an integer
* Return: Always 0
*/
int _abs(int n)
{
	if (n < 0)
		n = -n;
		return (n);
}
